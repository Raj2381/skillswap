'use client'
import {useMemo,useState} from 'react'; import {Search,SlidersHorizontal} from 'lucide-react'; import {creators,categories,skills} from '@/src/data/mock'; import {Layout,CreatorCard} from '@/components/client-shell';
export default function FindCreators(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const filtered=useMemo(()=>creators.filter(c=>(cat==='All'||c.category===cat)&&[c.name,c.username,c.bio,c.category,...c.skills].join(' ').toLowerCase().includes(q.toLowerCase())),[q,cat]);return <Layout title="Find your next creator"><div className="flex flex-col gap-4 md:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-3.5 size-5 text-[#9F8189]"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search creators, skills and services..." className="w-full rounded-xl border border-[#6A2636] bg-[#1A080E] py-3 pl-12 pr-4 outline-none"/></div><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded-xl border border-[#6A2636] bg-[#1A080E] px-4 text-sm"><option>All</option>{categories.map(c=><option key={c}>{c}</option>)}</select></div><div className="mt-4 flex flex-wrap gap-2">{skills.slice(0,10).map(s=><button key={s} onClick={()=>setQ(s)} className="rounded-full border border-[#3A101C] px-3 py-1.5 text-xs text-[#C7A7B0] hover:border-[#A52546]">{s}</button>)}</div><p className="mt-10 text-sm text-[#9F8189]"><SlidersHorizontal className="mr-2 inline size-4"/>{filtered.length} creators available</p><div className="mt-4 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(c=><CreatorCard key={c.id} creator={c}/>)}</div></Layout>}
