import {creatorById,services} from '@/src/data/mock'; import {Layout,ServiceCard} from '@/components/client-shell'; export default async function Services({params}:{params:Promise<{creatorId:string}>}){const {creatorId}=await params;const c=creatorById(creatorId);return <Layout title={`${c.name}'s services`} back><div className="grid gap-5 md:grid-cols-3">{services.filter(s=>s.creatorId===c.id).map(s=><ServiceCard key={s.id} service={s}/>)}</div></Layout>}
