'use client'
import {useState} from 'react'; import {projects} from '@/src/data/mock'; import {Layout,ProjectCard} from '@/components/client-shell'; export default function MyProjects(){const [tab,setTab]=useState('All');const tabs=['All','In Progress','Review','Completed','Cancelled'];const shown=projects.filter(p=>tab==='All'||p.status===tab);return <Layout title="My projects"><div className="flex flex-wrap gap-2">{tabs.map(t=><button key={t} onClick={()=>setTab(t)} className={`rounded-full px-4 py-2 text-sm ${tab===t?'bg-[#A52546] text-white':'border border-[#3A101C] text-[#C7A7B0]'}`}>{t}</button>)}</div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{shown.map(p=><ProjectCard key={p.id} project={p}/>)}</div></Layout>}
