import Link from 'next/link'; import {categories,skills} from '@/src/data/mock'; import {Layout,card,button} from '@/components/client-shell'; export default function Categories(){return <Layout title="Browse skills and categories"><p className="max-w-2xl text-[#C7A7B0]">Find specialists across every part of your next project.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((c,i)=><Link href={`/find-creators?category=${encodeURIComponent(c)}`} key={c} className={`${card} p-6 transition hover:-translate-y-1 hover:border-[#A52546]`}><p className="text-xs text-[#D66A84]">{String(i+1).padStart(2,'0')}</p><h2 className="mt-4 text-xl font-semibold">{c}</h2><div className="mt-4 flex flex-wrap gap-2">{skills.slice(i%5,(i%5)+4).map(s=><span key={s} className="text-xs text-[#9F8189]">{s}</span>)}</div><span className={`${button} mt-6`}>Explore</span></Link>)}</div></Layout>}
