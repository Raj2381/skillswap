'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {Layout,card,button} from '@/components/client-shell'; export default function EditProfile(){const [saved,setSaved]=useState(false);const router=useRouter();return <Layout title="Edit profile" back><form onSubmit={e=>{e.preventDefault();setSaved(true);setTimeout(()=>router.push('/profile'),400)}} className={`${card} max-w-2xl p-6`}>{[['Full name','Sristhi Ray'],['Username','@sristhiray'],['Email','sristhi@example.com'],['Location','Mumbai, India'],['Company','Independent creator']].map(([l,v])=><label key={l} className="mb-5 block text-sm">{l}<input defaultValue={v} className="mt-2 w-full rounded-xl border border-[#6A2636] bg-[#1A080E] p-3 outline-none"/></label>)}<label className="block text-sm">Bio<textarea defaultValue="Building thoughtful products with talented people." className="mt-2 min-h-28 w-full rounded-xl border border-[#6A2636] bg-[#1A080E] p-3 outline-none"/></label><button className={`${button} mt-6`}>{saved?'Saved':'Save changes'}</button></form></Layout>}
