import { DashboardShell, SectionTitle, card } from '@/components/creator/creator-shell'
export default function SettingsPage(){return <DashboardShell><SectionTitle title="Settings" subtitle="Control your notifications and workspace preferences."/><div className={`${card} max-w-2xl divide-y divide-[#eeece7]`}>{[['Email notifications','Get updates about requests and messages.'],['Project reminders','Receive reminders before deadlines.'],['Public profile','Allow clients to discover your profile.']].map(([title,description])=><label className="flex items-center justify-between gap-4 py-5 first:pt-0 last:pb-0" key={title}><span><span className="block text-sm font-semibold">{title}</span><span className="mt-1 block text-xs text-[#99958e]">{description}</span></span><input type="checkbox" defaultChecked className="h-4 w-4 accent-[#96734f]"/></label>)}</div></DashboardShell>}
