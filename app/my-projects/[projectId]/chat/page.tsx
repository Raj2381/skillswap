import {projectById,creatorById} from '@/src/data/mock'; import {Layout,ChatBox} from '@/components/client-shell'; export default async function Chat({params}:{params:Promise<{projectId:string}>}){const {projectId}=await params;const p=projectById(projectId);return <Layout title={`Chat with ${creatorById(p.creatorId).name}`} back><div className="mx-auto max-w-3xl"><div className="mb-4 flex flex-wrap gap-2">{['Ask for update','Request revision','Approve delivery'].map(x=><button key={x} className="rounded-full border border-[#6A2636] px-3 py-2 text-xs text-[#D9A4B0]">{x}</button>)}</div><ChatBox projectId={projectId}/></div></Layout>}
